//This program is to find the area of a square

#include <stdio.h>

int main(){
	int sidelength;
	printf("Enter side length of square: ");
	scanf("%d", &sidelength);
	printf("The area of the square is : %d", sidelength*sidelength);
	return 0;
}
